<div class="admin-inner-content">
  <div class="admin-inner-content-title">New Information</div>
  <div class="admin-inner-content-detail">
    <%= raw msg_flash(flash) %>
    <%= form_for([:admin, @information], :url => admin_informations_path,
                 :html => { :multipart => true}) do |f| %>
      <%= f.error_messages %>
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field button">
        <%= f.submit 'Create Information' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_informations_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>
